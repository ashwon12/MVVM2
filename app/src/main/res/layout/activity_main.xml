<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.view.View" />
        <import type="android.widget.EditText"/>
        <variable
            name="viewModel"
            type="com.example.mvvm2.viewmodel.MovieViewModel" />

        <variable
            name="activity"
            type="com.example.mvvm2.ui.view.MainActivity" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout  xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:id="@+id/et_keyword"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="8dp"
            android:background="#00000000"
            android:hint="@string/search_hint"
            android:text="@={viewModel.query}"
            android:inputType="text"
            android:textSize="18dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btn_search"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="3dp"
            android:text="@string/serach_button"
            android:onClick="@{() -> viewModel.getSearchResponseList()}"
            app:layout_constraintBottom_toBottomOf="@+id/et_keyword"
            app:layout_constraintEnd_toStartOf="@id/btn_log"
            app:layout_constraintTop_toTopOf="@+id/et_keyword" />
        
        <Button
            android:id="@+id/btn_log"
            android:layout_width="0dp"
            app:layout_constraintTop_toTopOf="@id/btn_search"
            android:layout_height="wrap_content"
            android:text="@string/log_button"
            android:layout_marginEnd="8dp"
            android:onClick="@{(v)-> viewModel.logClick(v)}"
            app:layout_constraintBottom_toBottomOf="@id/btn_search"
            app:layout_constraintEnd_toEndOf="parent"/>

        <View
            android:id="@+id/view"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="3dp"
            android:background="@color/black"
            app:layout_constraintTop_toBottomOf="@id/btn_search" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_movie"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:orientation="vertical"
            android:layout_marginTop="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view"
            bind_movieList="@{viewModel.movieList}"
            />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:gravity="center"
            android:text="@string/empty_text"
            android:visibility="@{viewModel.emptyText ? View.VISIBLE : View.INVISIBLE}"
            app:layout_constraintTop_toBottomOf="@id/view"
            app:layout_constraintBottom_toBottomOf="parent"
           />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>